export enum CouponType {
  ABSOLUTE = "ABSOLUTE",
  FIXED = "FIXED",
  PERCENTAGE = "PERCENTAGE",
}

export enum CouponCategory {
  ALL = "ALL",
  AGE_GROUP = "AGE_GROUP",
  DFS = "DFS",
}
